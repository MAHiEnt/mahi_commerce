// This file serves your product list.
// It replaces the hardcoded array in the HTML.
const products = require('../../products.json');

exports.handler = async () => {
  return {
    statusCode: 200,
    body: JSON.stringify(products),
  };
};
